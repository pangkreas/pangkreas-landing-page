# Public frontend configuration only. Never place secrets here.
# Full POST URL. Leave empty to hide the form.
VITE_CONTACT_API_URL=
# Only configure public channels verified by the Pangkreas owner.
VITE_CONTACT_EMAIL=
# International number format, for example +62... (example only, not a real number).
VITE_CONTACT_WHATSAPP=
VITE_ANALYTICS_ENABLED=false
